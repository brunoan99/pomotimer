import { DialogDemo } from "@/components/settings-dialog"

const Home = () => (
  <div className="w-screen p-8 flex flex-row justify-between">
    <h1>PomoTimer</h1>
    <DialogDemo />
  </div>
)

export { Home }
